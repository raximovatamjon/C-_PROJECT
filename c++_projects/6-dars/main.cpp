#include <iostream>

using namespace std;

int main()
{


    ///1. Rostlikga tekshiring a va b sonlari berilgan. a soni b sonidan 30 ga katta


    /*
    int a,b,c;

    cout << "a  son kiriting : ";
    cin >> a;


    cout << "b  son kiriting : ";
    cin >> b;




    bool tek = ( a * 30 == b );


    cout << boolalpha << "natija " << tek << endl;
    */








    ///2. Rostlikga tekshiring. a , b , c , d sonlari berilgan. b soni eng katta va juft son.

    /*
    int a,b,c,d;

    cout << "a  son kiriting : ";
    cin >> a;

    cout << "b  son kiriting : ";
    cin >> b;

    cout << "c  son kiriting : ";
    cin >> c;


    cout << "d  son kiriting : ";
    cin >> d;


    bool tek = ( b > a && b > c && b > d && b % 2 == 0 );


    cout << boolalpha << "natija " << tek << endl;
    */



    ///3. Rostlikga tekshiring. a , b , c , d sonlari berilgan. c soni eng kichik va juft.


    /*
    int a,b,c,d;

    cout << "a  son kiriting : ";
    cin >> a;

    cout << "b  son kiriting : ";
    cin >> b;

    cout << "c  son kiriting : ";
    cin >> c;


    cout << "d  son kiriting : ";
    cin >> d;


    bool tek = ( c < a && c < b && c < d && c % 2 == 0 );


    cout << boolalpha << "natija " << tek << endl;
    */















    main();
    return 0;
}
